import { createSlice } from "@reduxjs/toolkit";

type ProductsType = {
    items:
}

const ProductList = {
  items: [],
};

const productsSlice = createSlice({
  name: "products",
  ProductList,
  reducers: (state, action) => {},
});
